apiVersion: v2
name: logistics-ml-platform
description: Helm chart for the logistics ML platform (postgres, kafka, flink, mlflow, training, etl, api, dozzle)
type: application
version: 0.1.0
appVersion: "1.0.0"
